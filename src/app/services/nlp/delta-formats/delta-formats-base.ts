// import { Delta } from 'quill';
// import { BlotTypes } from '../../enums/blot-types.enum';

// export abstract class DeltaFormatsBase {
//     protected delta: Delta;
//     protected text: string;
//     protected parentLeafType: BlotTypes;
//     protected rightIndex: number;

//     constructor(delta: Delta, text: string, parentLeafType: BlotTypes, rightIndex?: number) {
//         this.delta = delta;
//         this.text = text;
//         this.parentLeafType = parentLeafType;
//         this.rightIndex = rightIndex;
//     }
// }